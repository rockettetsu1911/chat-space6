.side__bar
  .side__bar--header
    .side__bar--header--box
      .user__name
        = current_user.name
      %ul.menu
        %li.new__group
          = link_to "#" do
            = icon('fas', 'edit')
        %li.edit__user
          = link_to edit_user_path(current_user) do
            = icon('fas', 'cog')
  .groups
    .group
      %p.group__name
        techcamp
      %p.group__text
        まだメッセージがありません。
    .group
      %p.group__name
        techcamp
      %p.group__text
        まだメッセージがありません。

