.side__bar
  .side__bar--header
    .side__bar--header--box
      .user__name
        = current_user.name
      %ul.menu
        %li.new__group
          = link_to new_group_path do
            = icon('fas', 'edit')
        %li.edit__user
          = link_to edit_user_path(current_user) do
            = icon('fas', 'cog')
  .groups
    - current_user.groups.each do |group|
      .group
        = link_to '#' do
          %p.group__name
            = group.name
          %p.group__text
            まだメッセージがありません。
